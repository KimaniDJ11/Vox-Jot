# Vox Jot speech-analysis runtime dependencies.
#
# This environment is intentionally separate from speech-runtime/.venv.
# It covers file-transcription ASR, speaker diarization, ONNX, and CoreML.
# MLX-Audio and Gemma audio use separate app-managed venvs.

torch==2.13.0
torchaudio==2.11.0
torchvision==0.28.0
torchcodec==0.15.0

transformers==5.5.0
huggingface-hub==1.5.0
librosa==0.11.0
soundfile==0.13.1

pyannote.audio==4.0.4
whisperx==3.4.2
# NeMo Sortformer is intentionally excluded until NeMo supports
# transformers>=5.5.0. The latest tested NeMo line still requires vulnerable
# Transformers 4.x pins, so the app disables the NeMo catalog row instead of
# shipping an insecure shared runtime.

onnx==1.22.0
onnxruntime==1.25.1
coremltools==9.0

# Speech emotion recognition (emotion2vec+) runs through FunASR's AutoModel.
funasr==1.3.9
