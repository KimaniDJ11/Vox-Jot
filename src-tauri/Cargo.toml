[package]
name = "vox_jot"
version = "1.0.4"
description = "Vox Jot"
authors = ["Irie Dinamik", "cjpais"]
edition = "2021"
license = "MIT"
default-run = "vox_jot"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("cargo-clippy", "ci-mock-transcription"))'] }

[features]
default = ["speech-runtime"]
speech-runtime = ["dep:transcribe-rs"]
ci-mock-transcription = []

[profile.dev]
incremental = true # Compile your binary in smaller steps.

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "vox_jot_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.0", features = [] }
serde_json = "1"
serde = { version = "1", features = ["derive"] }

[dependencies]
once_cell = "1"
tauri = { version = "2.11.1", features = [
  "macos-private-api",
  "tray-icon",
  'image-png',
] }
tauri-plugin-log = "2.8.0"
tauri-plugin-opener = "2.5.3"
tauri-plugin-store = "2.4.3"
tauri-plugin-os = "2.3.2"
tauri-plugin-clipboard-manager = "2.3.2"
tauri-plugin-macos-permissions = "2.3.0"
tauri-plugin-process = "2.3.1"
rusqlite_migration = "2.3"
tauri-plugin-fs = "2.5.1"
tauri-plugin-localhost = "2.3.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
plist = "1.9"
sha2 = "0.10"
rdev = { git = "https://github.com/rustdesk-org/rdev" }
cpal = "0.16.0"
anyhow = "1.0.95"
rubato = "0.16.2"
hound = "3.5.1"
log = "0.4.25"
env_filter = "1.0.1"
tokio = { version = "1.43.0", features = ["process"] }
vad-rs = { git = "https://github.com/cjpais/vad-rs", default-features = false }
enigo = "0.6.1"
rodio = { git = "https://github.com/cjpais/rodio.git" }
reqwest = { version = "0.12", features = ["json", "stream", "blocking", "multipart"] }
ureq = { version = "3.3", features = ["multipart"] }
dirs = "6"
futures-util = "0.3"
rustfft = "6.4.0"
nnnoiseless = { version = "0.5.2", default-features = false }
similar = "2"
strsim = "0.11.0"
uuid = { version = "1", features = ["v4"] }
natural = "0.5.0"
regex = "1"
chrono = "0.4"
rusqlite = { version = "0.37", features = ["bundled"] }
pdf-extract = "0.10.0"
quick-xml = "0.39.2"
tar = "0.4.46"
flate2 = "1.0"
bzip2 = "0.5"
zip = "0.6"
handy-keys = "0.2.4"
ferrous-opencc = "0.4.0"
clap = { version = "4", features = ["derive"] }
specta = "=2.0.0-rc.22"
specta-typescript = "0.0.9"
tauri-specta = { version = "=2.0.0-rc.21", features = ["derive", "typescript"] }
tauri-plugin-dialog = "2.7.0"
base64 = "0.22"
keyring-core = "1"
notify = "6"
notify-debouncer-full = "0.3"
axum = { version = "0.7", default-features = false, features = ["http1", "json", "tokio", "multipart"] }
sentry = { version = "0.48.2", default-features = false, features = ["backtrace", "contexts", "native-tls", "panic", "reqwest"] }

[target.'cfg(unix)'.dependencies]
signal-hook = "0.3"

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-autostart = "2.5.1"
tauri-plugin-global-shortcut = "2.3.1"
tauri-plugin-single-instance = "2.4.2"
tauri-plugin-updater = "2.10.0"

[target.'cfg(windows)'.dependencies]
windows-native-keyring-store = "1"
windows = { version = "0.61.3", features = [
  "Win32_Media_Audio_Endpoints",
  "Win32_System_Com",
  "Win32_System_Com_StructuredStorage",
  "Win32_System_Variant",
  "Win32_Foundation",
  "Win32_UI_WindowsAndMessaging",
  "Win32_UI_Accessibility",
  "Win32_UI_HiDpi",
  "Win32_System_Threading",
  "Win32_System_ProcessStatus",
  "Win32_Storage_FileSystem",
  "Win32_Graphics_Gdi",
  "Foundation",
  "Foundation_Collections",
  "Globalization",
  "Graphics_Imaging",
  "Media_Ocr",
  "Storage_Streams",
] }
winreg = "0.56"
uiautomation = "0.10"
window-vibrancy = "0.5"

[target.'cfg(target_os = "macos")'.dependencies]
apple-native-keyring-store = { version = "1", features = ["keychain"] }
transcribe-rs = { version = "0.3.11", default-features = false, features = ["onnx", "whisper-cpp", "whisper-metal"], optional = true }
tauri-nspanel = { git = "https://github.com/ahkohd/tauri-nspanel", branch = "v2.1" }
accessibility = "0.1"
accessibility-sys = "0.1"
core-foundation = { version = "0.10", features = ["mac_os_10_8_features"] }
core-graphics = "0.24"
window-vibrancy = "0.5"
objc = "0.2"

[target.'cfg(target_os = "linux")'.dependencies]
dbus-secret-service-keyring-store = { version = "1", features = ["crypto-rust"] }
transcribe-rs = { version = "0.3.11", default-features = false, features = ["onnx", "whisper-cpp"], optional = true }
png = "0.17"
# atspi is excluded until the Linux field monitor is implemented

[target.'cfg(not(any(target_os = "macos", target_os = "linux")))'.dependencies]
transcribe-rs = { version = "0.3.11", default-features = false, features = ["onnx", "whisper-cpp"], optional = true }

[dev-dependencies]
tempfile = "3"
tokio = { version = "1.43.0", features = ["macros", "rt-multi-thread", "time"] }

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "unwind"
