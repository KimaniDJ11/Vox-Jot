<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.device.microphone</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Required to load the bundled ONNX Runtime, whisper.cpp, and Python
         runtime dylibs, which are not signed by this team. It weakens the
         hardened runtime: any validly-signed dylib may load into the process.
         Remove this if every bundled dylib is ever re-signed by this team. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
  </dict>
</plist>
