{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-overlay",
  "description": "Minimal capability for the recording overlay window.",
  "windows": ["recording_overlay"],
  "permissions": [
    "core:default",
    "store:default",
    "core:window:allow-start-dragging",
    "recording-overlay-commands"
  ]
}
